## YAML Template.

default:
    autoload:
        - %paths.base%/tests/features
        - %paths.base%/src
        
    suites:
        recaptcha:
            paths: [%paths.base%/tests/gherkin/dummy.feature]
            contexts: [FeatureContext]